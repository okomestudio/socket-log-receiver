#!/usr/bin/env bash

set -e

pip install --force-reinstall --no-cache-dir -e .

pytest -vv \
       --cov=src/socket_log_receiver \
       --cov-report=term-missing \
       --log-level=DEBUG \
       tests/
